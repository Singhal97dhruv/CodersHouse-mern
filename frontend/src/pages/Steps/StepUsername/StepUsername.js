import React from 'react'

const StepUsername = ({onNext}) => {
  return (
    <>

    <div>Username Componnet</div>
    <button onClick={onNext}>Click ME</button>

   </>
  )
}

export default StepUsername
